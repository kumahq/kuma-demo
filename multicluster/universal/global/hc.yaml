type: HealthCheck
name: gateway-to-backend
mesh: default
sources:
- match:
    kuma.io/service: 'gateway'
destinations:
- match:
    kuma.io/service: 'backend'
conf:
  interval: 1s
  timeout: 1s
  unhealthyThreshold: 3
  healthyThreshold: 1

