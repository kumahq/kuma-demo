type: TrafficRoute
mesh: default
name: tr-all-to-cluster2
sources:
  - match:
      service: gateway
destinations:
  - match:
      service: backend
conf:
  - weight: 100
    destination:
      service: backend
      cluster: "cluster-2"
