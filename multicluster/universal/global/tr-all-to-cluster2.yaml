type: TrafficRoute
mesh: default
name: tr-all-to-cluster2
sources:
  - match:
      kuma.io/service: gateway
destinations:
  - match:
      kuma.io/service: backend
conf:
  - weight: 100
    destination:
      service: backend
      cluster: "cluster-2"
